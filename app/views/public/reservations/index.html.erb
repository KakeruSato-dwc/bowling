<% @reservations.each do |reservation| %>
  <%= reservation.created_at %>
  <%= reservation.group_name %>
  <%= reservation.num_children + reservation.num_students + reservation.num_adults %>
  <%= reservation.start_date %>
<% end %>
